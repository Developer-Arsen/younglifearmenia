# Build output
# dist/ is the deploy artifact for wrangler; it must be committed
# unless the Cloudflare build command runs ./build.sh first.
dist/
node_modules/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
